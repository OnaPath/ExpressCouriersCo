const CONFIG = {
    // Use your production Google Places API key here
    GOOGLE_PLACES_API_KEY: process.env.GOOGLE_PLACES_API_KEY,
    
    // Production API endpoints (if needed)
    API_BASE_URL: process.env.API_BASE_URL || 'https://api.expresscouriers.co',
    
    // Production-specific settings
    DEBUG: false,
    ENVIRONMENT: 'production'
}; 